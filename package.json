{
  "name": "bigparsing_confirmed",
  "version": "1.0.0",
  "description": "The problem consists in extracting data from a JSON file too big to be parsed / hold in memory as a whole.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.0",
    "fs": "0.0.1-security",
    "JSONStream": "^1.3.5"
  },
  "dependencies": {
    "typescript": "^4.9.3"
  }
}
